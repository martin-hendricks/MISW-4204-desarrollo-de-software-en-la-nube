name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  PYTHON_VERSION: '3.11'

jobs:
  # Etapa 1: Ejecución de pruebas unitarias
  test:
    name: Run Unit Tests
    runs-on: ubuntu-latest
    
    services:
      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Cache pip dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('source/**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg

    - name: Install backend dependencies
      run: |
        cd source/backend
        pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest-cov
        
        # Verificar que las dependencias se instalaron correctamente
        echo "=== Backend Dependencies Check ==="
        pip list | grep -E "(fastapi|pytest|sqlalchemy|psycopg2)"
        echo "=================================="

    - name: Install worker dependencies
      run: |
        cd source/worker
        pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run backend tests (SQLite)
      run: |
        cd source/backend
        export DATABASE_URL="sqlite:///./test.db"
        export REDIS_URL="redis://localhost:6379/0"
        export TEST_MODE=true
        export UPLOAD_DIR="/tmp/test_uploads"
        mkdir -p /tmp/test_uploads/original /tmp/test_uploads/processed
        
        # Crear las tablas de la base de datos
        python3 -c "
        from app.infrastructure.database.database import engine, Base
        from app.infrastructure.database import models
        Base.metadata.create_all(bind=engine)
        print('✅ Database tables created successfully')
        "
        
        # Ejecutar las pruebas usando app_test_main (que ya tiene la configuración correcta)
        pytest tests/ -v --cov=app --cov-report=xml --cov-report=html

    - name: Run worker tests (SQLite)
      run: |
        cd source/worker
        export DATABASE_URL="sqlite:///./test.db"
        export REDIS_URL="redis://localhost:6379/0"
        export UPLOAD_DIR="/tmp/test_uploads"
        pytest tests/ -v --cov=. --cov-report=xml --cov-report=html

    - name: Upload coverage reports
      uses: codecov/codecov-action@v3
      with:
        files: ./source/backend/coverage.xml,./source/worker/coverage.xml
        flags: unittests
        name: codecov-umbrella

  # Etapa 2: Construcción automática de la aplicación
  build:
    name: Build Application
    runs-on: ubuntu-latest
    needs: test
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build backend image
      uses: docker/build-push-action@v5
      with:
        context: ./source/backend
        push: false
        tags: misw4204-backend:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Build worker image
      uses: docker/build-push-action@v5
      with:
        context: ./source/worker
        push: false
        tags: misw4204-worker:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Build API Gateway image
      uses: docker/build-push-action@v5
      with:
        context: ./source/api-gateway
        push: false
        tags: misw4204-api-gateway:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Build database image
      uses: docker/build-push-action@v5
      with:
        context: ./source/database
        push: false
        tags: misw4204-database:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Test Docker Compose
      run: |
        # Verificar que docker compose puede construir todos los servicios
        docker compose -f source/docker-compose.yml config
        echo "Docker Compose configuration is valid"

  # Etapa 3: Validación de calidad del código con SonarCloud
  sonarcloud:
    name: Code Quality Analysis
    runs-on: ubuntu-latest
    needs: test
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg

    - name: Install dependencies
      run: |
        cd source/backend
        pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest-cov
        cd ../worker
        pip install -r requirements.txt

    - name: Run tests with coverage
      run: |
        # Backend tests with coverage
        cd source/backend
        export DATABASE_URL="sqlite:///./test.db"
        export REDIS_URL="redis://localhost:6379/0"
        export TEST_MODE=true
        export UPLOAD_DIR="/tmp/test_uploads"
        mkdir -p /tmp/test_uploads/original /tmp/test_uploads/processed
        pytest tests/ --cov=app --cov-report=xml --cov-report=term

        # Worker tests with coverage
        cd ../worker
        export DATABASE_URL="sqlite:///./test.db"
        export REDIS_URL="redis://localhost:6379/0"
        export UPLOAD_DIR="/tmp/test_uploads"
        pytest tests/ --cov=. --cov-report=xml --cov-report=term

    - name: Check SonarCloud Token
      id: check_token
      run: |
        if [ -n "${{ secrets.SONAR_TOKEN }}" ]; then
          echo "has_token=true" >> $GITHUB_OUTPUT
          echo "✅ SonarCloud token found - will run analysis"
        else
          echo "has_token=false" >> $GITHUB_OUTPUT
          echo "⚠️ No SonarCloud token found - skipping analysis"
        fi

    - name: SonarCloud Scan
      if: steps.check_token.outputs.has_token == 'true'
      uses: SonarSource/sonarqube-scan-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

    - name: Skip SonarCloud Analysis
      if: steps.check_token.outputs.has_token == 'false'
      run: |
        echo "⚠️ SonarCloud analysis skipped - no token configured"
        echo "To enable SonarCloud analysis:"
        echo "1. Go to https://sonarcloud.io"
        echo "2. Sign in with GitHub"
        echo "3. Generate a token"
        echo "4. Add SONAR_TOKEN secret in GitHub repository settings"

  # Job de notificación (opcional)
  notify:
    name: Notify Results
    runs-on: ubuntu-latest
    needs: [test, build, sonarcloud]
    if: always()
    
    steps:
    - name: Notify Success
      if: ${{ needs.test.result == 'success' && needs.build.result == 'success' }}
      run: |
        echo "✅ All CI/CD stages completed successfully!"
        echo "✅ Unit tests passed"
        echo "✅ Application built successfully"
        if [ "${{ needs.sonarcloud.result }}" == "success" ]; then
          echo "✅ Code quality analysis passed"
        else
          echo "⚠️ Code quality analysis skipped (SonarCloud not configured)"
        fi

    - name: Notify Failure
      if: ${{ needs.test.result == 'failure' || needs.build.result == 'failure' }}
      run: |
        echo "❌ CI/CD pipeline failed!"
        echo "Test result: ${{ needs.test.result }}"
        echo "Build result: ${{ needs.build.result }}"
        if [ "${{ needs.sonarcloud.result }}" == "failure" ]; then
          echo "SonarCloud result: ${{ needs.sonarcloud.result }}"
        fi
        exit 1
