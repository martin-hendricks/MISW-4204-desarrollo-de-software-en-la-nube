# ==============================================================================
# CONFIGURACIÓN DEL PRODUCER PARA PRUEBAS DE CARGA
# ==============================================================================

# ------------------------------------------------------------------------------
# BROKER DE MENSAJES (SQS o Redis)
# ------------------------------------------------------------------------------

# Usar AWS SQS como broker (true) o Redis (false)
USE_SQS=true

# AWS SQS Configuration (cuando USE_SQS=true)
AWS_REGION=us-east-1
SQS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/ACCOUNT_ID/anb-video-processing-queue
SQS_DLQ_URL=https://sqs.us-east-1.amazonaws.com/ACCOUNT_ID/anb-video-processing-dlq

# Redis Configuration (cuando USE_SQS=false, para desarrollo local)
# REDIS_URL=redis://localhost:6379/0

# ------------------------------------------------------------------------------
# ALMACENAMIENTO DE ARCHIVOS (S3 o Local)
# ------------------------------------------------------------------------------

# Usar AWS S3 para almacenamiento (true) o volumen compartido local (false)
USE_S3=true

# AWS S3 Configuration (cuando USE_S3=true)
S3_BUCKET_NAME=your-bucket-name

# ------------------------------------------------------------------------------
# CREDENCIALES DE AWS
# ------------------------------------------------------------------------------
# Nota: Si usas IAM roles en EC2, estas variables no son necesarias
# Para AWS Academy, usa las credenciales temporales del laboratorio

AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key
AWS_SESSION_TOKEN=your_session_token

# ------------------------------------------------------------------------------
# NOTAS DE CONFIGURACIÓN
# ------------------------------------------------------------------------------
# 1. Para obtener las URLs de SQS, ejecuta el script setup-sqs.sh:
#    cd ../../deployment/sqs-setup
#    ./setup-sqs.sh us-east-1
#
# 2. Las credenciales de AWS Academy se encuentran en:
#    AWS Details > AWS CLI > Show
#
# 3. Si ejecutas el producer desde una instancia EC2 con IAM Role,
#    no necesitas configurar las credenciales manualmente
